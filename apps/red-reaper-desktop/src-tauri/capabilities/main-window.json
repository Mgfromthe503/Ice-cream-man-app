{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability for the Red Reaper AI command center. File, shell, process, network, and sidecar permissions are intentionally absent.",
  "windows": ["main"],
  "platforms": ["windows", "linux", "macOS"],
  "permissions": ["core:default"]
}
