[package]
name = "red-reaper-ai"
version = "0.1.0"
description = "Local-first desktop command center for Red Reaper AI"
authors = ["Red Reaper AI contributors"]
edition = "2021"
license = "MIT"

[lib]
name = "red_reaper_ai_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri = { version = "2.0", features = [] }
