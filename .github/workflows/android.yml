# Disabled: this is an Expo / React Native app, not a root-level Android Gradle project.
# Native Android builds go through EAS (see eas-build.yml), not a bare ./gradlew at repo root.
# Re-enable only if you eject or add a committed android/ folder with gradlew.
name: Android CI

on:
  workflow_dispatch:  # manual only — do not run on push/PR

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "Android CI is disabled for this Expo project."
          echo "Use the EAS Build workflow instead."
