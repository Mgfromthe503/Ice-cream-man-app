# Disabled: this app is not published as an npm package (uses pnpm, private app).
# GitHub template would fail on npm ci / npm publish.
name: Node.js Package

on:
  workflow_dispatch:  # manual only — was release-triggered; keep available if needed later

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "npm publish workflow disabled for this private Expo app."
