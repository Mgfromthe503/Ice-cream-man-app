# Disabled: this project uses Expo Metro, not Webpack.
# The GitHub "NodeJS with Webpack" template always fails here (no webpack.config).
name: NodeJS with Webpack

on:
  workflow_dispatch:  # manual only — do not run on push/PR

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "Webpack workflow is disabled. This app uses Expo Metro."
