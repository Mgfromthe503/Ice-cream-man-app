# Disabled: JFrog SAST requires a paid JFrog subscription and access token.
# Keeping this file as a placeholder so the integration can be re-enabled later if needed.

name: "JFrog SAST Scan (Disabled)"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: JFrog SAST is disabled
        run: echo "JFrog SAST workflow is disabled for this repository."
